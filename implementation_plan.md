# IMPLEMENTATION PLAN - CHANTEOS REDESIGN

## Goal Description
Implement the "Sonic Obsidian" design system across the entire application, replacing the current UI with a premium, dark-mode-first aesthetic generated by Stitch.

## User Review Required
> [!IMPORTANT]
> This is a complete UI overhaul. Existing CSS variables in `index.css` will be updated to match the new color palette (`zinc-950`, `violet-400`).

## Proposed Changes

### 1. Global Styles & Theme
#### [MODIFY] [index.css](file:///d:/Appz/chanteos/src/index.css)
- Update CSS variables for `background`, `foreground`, `card`, `popover`, `primary`, `secondary`, `muted`, `accent`, `destructive`, `border`, `input`, `ring`.
- Enforce `dark` mode defaults.
- Ensure consistent font usage (Inter/Geist).

### 2. Main Dashboard (Song List)
#### [MODIFY] [SongList.tsx](file:///d:/Appz/chanteos/src/components/SongList.tsx)
- Implement sticky header with new layout.
- Redesign Song Cards to match "Sonic Obsidian" style (minimal, dark, metadata row).
- Update Empty State with "No songs yet" illustration.

### 3. Song Editor (Project Screen)
#### [MODIFY] [SongEditor.tsx](file:///d:/Appz/chanteos/src/components/SongEditor.tsx)
- Redesign top navigation bar (transparent title input).
- Update Lyric Block and Prompt Block styling (distinguishable prompts).
- Refactor bottom player bar to be fixed, glassmorphic, and contain all controls.

### 4. Settings Dialog
#### [MODIFY] [SettingsDialog.tsx](file:///d:/Appz/chanteos/src/components/SettingsDialog.tsx)
- Update dialog content to match the new Stitch design.
- Ensure all sections (Appearance, AI, Prompts) are present and styled correctly.

### 5. Components & UI
#### [MODIFY] [AudioPlayer.tsx](file:///d:/Appz/chanteos/src/components/AudioPlayer.tsx)
- Update player controls to match the new design (Loop A-B colors, layout).

## Verification Plan

### Manual Verification
- **Visual Check:**
    - Verify "Sonic Obsidian" colors (Zinc 950 background).
    - Check touch targets on mobile (min 44px).
    - Ensure sticky headers work on scroll.
- **Functional Check:**
    - Test Song creation, editing, and deletion.
    - Test Audio playback keys and loop states.
    - Test Settings persistence (Theme, API Key).
